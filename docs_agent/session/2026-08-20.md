# 2026-08-20 会话：M3.5 智能优化轮（工具层升级：复合动作原语 + 感知过滤 + 提示契约）

## Plan + Spec（已与用户确认）

### 背景

M3 真机闭环成立但"不智能"（reports/M3-C.md）：任务 3 砍树 22 步 / 212k tokens 预算耗尽。根因是**动作粒度太低**——VLM 被迫当小脑，每按一次 W 都花一次完整调用。对比参考项目（Numen / Mindcraft CE 精读报告见会话上下文，参考仓库在 `C:\AI\mc-projects\{minecraft-numen, mindcraft-ce, N.E.K.O}`）：两者均"LLM 只做意图层决策、执行下沉确定性代码"，砍树约 2 次 LLM 调用。

用户裁决：**多参考 Numen 工具实现**（模组兼容做法最好）；**寻路 = Baritone 集成**（前置验证，不可用回落自研 A* 并回报）；**反射层留给 M4**（本轮不做）。

### 验收标准

1. 重跑 M3-C 任务 3「搜集云杉木 5 根」：LLM 调用 ≤4 次、tokens ≤30k、finish 且复核挖到（world.query 原木减少 / 背包有原木）
2. 「来我这里」：1 次 walkTo 调用闭环（M3 为 11 步）
3. 急停在原语执行中 ≤1s 生效（Baritone #stop / 按键释放）
4. pytest 全绿（现 263 项不破）+ gradlew smoke 全绿 + mock 双人回归通过

### 架构决策（为什么）

- **原语同步阻塞 + 协作式取消**（非 Numen 式异步受理）：AgentLoop 本就串行执行工具，同步原语让 60s 行走只占 1 个 tool call 位、期间零 VLM 调用；急停通过微步内查取消标志保证 ≤1s。M5 分层时再升级异步受理（task/task_finished 协议帧与 TaskCard 模型已在 protocol/ 备好，不用动）
- **bridge 保持哑管道，只加两个向后兼容参数**（filter / hold_ms）：符合"输入标准化、感知原语化"既定分工；brain 侧组合一切
- **模组兼容零硬编码**（Numen 手段，`core/tools/ToolParse.java:37-56`、`core/init/InitTag.java:74-91`）：filter 走 registry name + `#tag` 前缀展开；不枚举 vanilla 名单
- **count 语义 = 目标方块挖除数**（Numen 是物品增量需背包基线，我们暂无背包快照工具，记可选增强）

### 任务分解表

| # | 任务 | 侧 | 交付物 | 验收 |
|---|---|---|---|---|
| T0 | session spec + Baritone 前置验证 | docs+环境 | 本文件；1.21.1 构建、HMCL 实例安装、#goto 冒烟 | #goto 驱动真客户端走 20 格；不可用停下回报用户 |
| T1 | bridge 感知/输入增强 | Java | world.query filter + input.click hold_ms；三点同步（pydantic→export_schema→Java Contracts）；schema v1.1 | gradlew smoke 通过；防漂移测试绿 |
| T2 | 原语模块 | brain | `sirius_brain/agent/primitives.py` | 单测绿（用 T4 mock 世界） |
| T3 | 提示与配置 | brain | 工具注册 + TOOL_HINTS + 系统提示重写 + 滚动状态注入 + 预算 500k + 错误码映射 | ScriptedVLM 测试断言提示含契约 |
| T4 | mock 世界模拟 | 测试 | MockBridgeServer 子类：可变 blocks + 坐标推进 + 假 Baritone | 原语离线端到端可测 |
| T5 | 真机验收 + 收尾 | 全 | 重跑任务 2/3 + 急停；reports/M3.5-*.md；PROGRESS/overall.md | 4 条验收 + 6 门禁 |

### 接口设计

**T1 bridge（向后兼容加参，schema v1.0→v1.1）**

`world.query` 参数新增 `filter?: string[]`（缺省 = 现行为不变）：
- blocks：条目为 registry name（`spruce_log` 自动补 `minecraft:` 前缀）或 `#tag`（`#minecraft:logs` → `BuiltInRegistries.BLOCK.getTag`/holder `is(tag)` 判定）；命中后**按与玩家距离平方升序**返回，cap 32，超出 `truncated:true`
- entities：条目为实体 type registry name；同样补齐 `truncated` 标志（现状缺——顺带修）
- 改动点：`ToolContracts.WorldQueryParams/worldQueryParams`（ToolContracts.java:101-139）、`scanBlocks`（:235-266）、`filterEntities`（:278-311）、`PerceptionTools.worldQuery`（PerceptionTools.java:193-240）

`input.click` 参数新增 `hold_ms?: int 0..10000`（缺省 = 旧行为 25ms tap 不变）：
- 有值时 PRESS → 调度器 +hold_ms 后 RELEASE（复用 M2-A input.key 的调度模式 InputTools.java:162-179）；`count` 与 `hold_ms` 互斥（同时给 → -32602）
- 改动点：`InputContracts.clickParams`（InputContracts.java:193 附近）、`InputTools` 点击执行

**T2 原语（`sirius_brain/agent/primitives.py`）**

```python
class Primitives:
    """任务级复合动作原语。Numen 式契约：意图进、结果出、失败带下一步建议。
    同步阻塞 + 协作式取消：微步循环（poll_interval≈0.5s）每步查 cancel()。"""

    def __init__(self, client, *, poll_interval=0.5): ...

    async def walk_to(self, x: float, z: float, y: float | None = None,
                      timeout: float = 120.0, cancel=None) -> ToolOutcome:
        # 1) command(f"#goto {x} {y or 当前y} {z}")   # 前缀#客户端拦截，不达服务器
        # 2) 轮询 getStats：水平距离 ≤2 → 成功（附最终坐标）
        # 3) 看门狗：15s 距离无进展 → 重发一次 #goto（只重发一次，Numen 近重试档）
        # 4) cancel() → 发 #stop → 返回"已中止（当前坐标）"
        # 5) 超时 → #stop + "路程仍在推进但超预算；同参数重发可续走，或改用更近途经点"（Numen 健康超时话术，MoveToCompanionTask.java:410-424）

    async def dig_block(self, x: int, y: int, z: int, timeout: float = 30.0, cancel=None):
        # 0) world.query(filter=[该坐标现方块]) 复核存在；已空 → 直接成功
        # 1) getStats 距离 >4.5 → "目标 x,y,z 距离 n 格超出触及范围；先 walkTo 到它旁边再挖"（Numen GoToThenDoTask.java:76-81 话术）
        # 2) 循环：lookAt(x+0.5, y+0.5, z+0.5) → input.click(button=0, hold_ms=600) → sleep(0.5)
        #    → world.query 复核消失 → 成功"已挖掉 minecraft:spruce_log (x,y,z)"
        # 3) 连续 8 段仍在 → "无法破坏该方块（可能被遮挡/工具不足/保护规则）；screenshot 观察或换目标"

    async def collect_block(self, block_ids: list[str], count: int, cancel=None):
        # 循环 min(count,…) 次：
        #   world.query(type=blocks, filter=block_ids) → 空 → break
        #   取最近 → walk_to(邻近，方块坐标±1.5) → dig_block → destroyed += 1
        # 收尾（Numen MineCompanionTask.java:886-897 契约）：
        #   destroyed ≥ count → "已挖到 n/count 个 <ids>"
        #   0 < destroyed < count → "已挖到 n/count；范围内已无更多，可接受或走远再试"（仍算成功）
        #   destroyed == 0 → 失败："范围 64 格内未找到 <ids>；确认 ID（含 #tag 写法）或走近些"
```

**T3 提示与配置**

- 工具描述写 Numen 式契约（含 BACKGROUND 段：受理即执行 / 失败读建议 / 超时同参重发续做）
- 系统提示重写：工具边界契约（world.query range≤64 + filter 用法、input.* 限频 20/s、gui-scaled 换算、原语优先/键鼠兜底的选用顺序）
- 滚动状态注入：run_task 每步 VLM 调用前 getStats 紧凑摘要（位置/血/饥饿/氧气）替换式注入一条 user 消息（不累积；Numen runtime_state 免费搭车做法，EntityAgentLoop.java:1452-1470）
- LoopConfig.max_total_tokens 200k→500k
- 错误码→建议映射：-32602 带"range 上限 64"提示；-32010 限频"稍等重试"；-32012 权限

**T4 mock 世界**

`FakeWorldBridge(MockBridgeServer)` 覆写 `_on_request` 维护可变状态：`position`、`blocks: dict[(x,y,z)→id]`；`getStats` 回 position；`world.query` 按 filter+range 从 blocks 表取；`lookAt` 记录朝向；`command("#goto …")` 启动假 Baritone 协程（每 tick 4.3 m/s 朝目标推进 position，到达停）；`command("#stop")` 停；`input.click(hold_ms)` 在触及+朝向对准时移除目标方块。原语单测全部离线跑此世界。

### 收尾日志（2026-08-20 补写）

#### 文件改动表

按子任务分组，逐文件细节见各报告 `docs_agent/reports/M3.5-*.md`（T0b/T1-java/T2T4/T3/T5a/T6/T7 + 两份 progress 防中断锚点）。

**sirius-brain（Python，pytest 263→302）**

| 文件 | 改动 | 为什么 |
|---|---|---|
| `sirius_brain/agent/primitives.py` | **新增**：`Primitives` 四原语 `walk_to`/`dig_block`/`collect_block`(+pickup 参数)/`pickup()`；常量注释即取值依据（WALK_* 看门狗与屏障 / DIG_* 递增 hold / PICKUP_* 礼仪与防死循环） | 把"走到/挖掉/收集 N 个"从 VLM 逐步键控下沉为一次调用内完成的确定性代码——本轮核心论点 |
| `sirius_brain/agent/tools.py` | 注册 walkTo/digBlock/collectBlock（`default_registry(cancel=None)` 共 14 工具）+ `PRIMITIVE_TOOL_HINTS` Numen 式契约描述 + CollectBlockParams.pickup | 原语进 VLM 工具表；描述即契约（受理即执行/失败读建议/同参重发续做），模型读文本就能自救 |
| `sirius_brain/agent/loop.py` | `_system_prompt` 重写（分层引导 + 工具边界契约 + 安全约束）；`_inject_rolling_status` 替换式状态注入；`bridge_error_hint` 错误码→建议映射；构造期 cancel 闭包接线 | 提示层三件事：告诉模型"原语优先/键鼠兜底"、每步免费搭车一份状态、报错文本带下一步动作 |
| `sirius_brain/agent/config.py` | `LoopConfig.max_total_tokens` 200k→500k；`VLMConfig.reasoning_effort` 新字段（env 围栏 `REASONING_EFFORT`） | 原语下沉后单任务调用数骤降，200k 误伤复杂任务；reasoning_effort 是本地 LM Studio 关思考唯一有效开关（部署细节见各机 local.md） |
| `sirius_brain/agent/vlm.py` | 请求体按需附 `reasoning_effort` 根级参数 | 同上：DashScope 用 enable_thinking、LM Studio 用 reasoning_effort，两开关并存按后端选用 |
| `sirius_brain/protocol/tools.py` | `WorldQueryParams.filter`、`ClickParams.hold_ms`、`LookAtParams.turn_speed_deg_s`、`DigParams` 新模型 | T1/T6 的 brain 半：pydantic 权威模型，schema 再导出 |
| `sirius_brain/protocol/export_schema.py` | `PROTOCOL_VERSION` 1.0→1.1（T1）→1.2（T6） | 协议演进随轮递增，index.json 是三处同步点之一 |
| `sirius_brain/bridge/config.py` | 默认 `protocol_version` "1.0"→"1.2" | T1-Java 遗留点由 T6 收口（hello 携带值三处一致） |
| `sirius_brain/mock/fakeworld.py` | **新增**：`FakeWorldBridge`——可变 position/blocks、假 Baritone 协程、#tag filter、dig 模拟（遮挡穿透）、掉落物生成与走近吸附 | 原语全链路离线可测；真机才挖得出的坑（截断/遮挡）在 mock 里可复现 |
| `sirius_brain/mock/server.py` | `tool_result` 钩子 + `ToolError` 错误帧通道 | 基类校验流水线单一来源，子类只换"算结果"一步；钩子抛错回 JSON-RPC 错误帧（-32601 fallback 测试用） |
| `schema/`（28 文件） | 再导出（含 `tools/dig.json`）；`index.json` protocol_version 1.2 | 冻结产物随模型同步；Java 侧构建期单向消费 |
| `tests/`（test_primitives 新增、test_agent_loop/test_bridge_client/test_schema_export 同步） | 263→302；另 8 处 `port=0` 端口纪律修正 | 见"问题与处理"③ |

**sirius-bridge（Java，smoke 241→345）**

| 文件 | 改动 | 为什么 |
|---|---|---|
| `ToolContracts.java` | `WorldQueryParams.filter` 校验归一化 + filtered 扫描（最近优先 32 + truncated，T6 改"收集→排序→截断"修 cap-before-sort）+ `EntityFact` 加 item/count + entities 补 truncated | 校验权威层（无 MC 类，smoke 可测）；截断排序修复见"问题与处理"⑤ |
| `PerceptionTools.java` | filter 分派 + `nameProbe`/`tagProbe`（TagKey memoize）+ item 实体注册名分支 | registry/TagKey 是 MC 类型只能在壳层解析；memo 使 tag 判定开销 = 每不同方块名一次 |
| `InputContracts.java` / `InputTools.java` | `hold_ms` 0..10000（与显式 count 互斥→-32602）；`clickSchedule` 纯函数；RELEASE 走 SCHEDULER 延迟调度 | 长按语义：PRESS 主线程内联、RELEASE 延迟——复用 M2-A input.key 的调度模式 |
| `DigContracts.java` / `DigTools.java` / `TurnController.java` | **新增（T6）**：dig 智能原语（前置校验/平滑瞄准 300deg/s/动作层监视按住/遮挡穿透/安全检查）+ tick 驱动平滑转头 | 事件层长按被 vanilla 焦点双门控废掉（见"问题与处理"④），操作型功能入 bridge 的先例 |
| `LookContracts.java` / `LookTools.java` | `turn_speed_deg_s` 30..720（null=瞬间转，字节级向后兼容）；新 turn 替换旧（supersede） | 固定角速度两轴同速推进、<1° 收口——"转头像自然转头"的用户裁决 |
| `SiriusBridge.java` / `BridgeServer.java` / `Capabilities.java` | tick 泵（TurnController/DigTools）；注册 dig；`PROTOCOL_VERSION` 1.1→1.2 | 三处同步点之二（Capabilities.java:41） |
| `SmokeMain.java` | 241→345（T1 +50、T6 +51、T7 +3） | 冒烟是 Java 侧唯一回归闸，新契约逐条钉死 |
| `README.md` | dig/平滑转头/截断语义/焦点限制（Unfocused-window behaviour 表补记） | 契约文档随码走 |

**探针与报告**：`m35_baritone_probe.py`（Baritone 冒烟）、`m35_realworld_probe.py`（T5a/T6 真机六/八项，选择性执行支持中断接续）、`m35_t7_pickup_probe.py`（T7 真机拾取）；`docs_agent/reports/M3.5-*.md` 7 份主报告 + T6/T7 两份 progress 锚点。探针属真机验收资产，随仓库保留（M2 m2_*.py 同例）。

#### 核心内容介绍

本轮论点：**运动控制从 VLM 下沉确定性代码**（Numen/Mindcraft 精读共识——LLM 只做意图层决策，执行交给代码）。M3 里 VLM 被迫当小脑（砍树 22 步、每按一次 W 一次完整调用、212k 预算耗尽）；M3.5 后一次 `collectBlock("#minecraft:logs", 3)` 调用内部完成"找最近→走位→挖掘→拾取"全链路，期间零 VLM 调用。

交付四大件：
1. **brain 任务级原语**：walkTo（Baritone #goto 封装 + 15s 看门狗 + 界面屏障 + 协作取消≤1s）、digBlock（bridge dig 优先，-32601 回退 hold 段循环）、collectBlock（Numen 收尾契约：有收获=成功；pickup 可配置）+ pickup()（未注册 VLM，M4 再议）
2. **bridge 智能原语**：dig（动作层监视按住，焦点免疫）、lookAt turn_speed_deg_s 平滑转头、world.query filter（registry id/#tag、最近优先 32）、input.click hold_ms、entities 载荷 item/count
3. **提示契约层**：系统提示分层引导（原语优先/键鼠兜底）+ 工具边界契约 + 滚动状态免费搭车 + 预算 500k + 错误码→建议映射
4. **协议 1.0→1.2**（1.1：filter/hold_ms；1.2：dig/turn_speed_deg_s/item-count），三处同步：schema index.json / Java Capabilities / brain bridge config

测试数字：pytest 263→302；Java 冒烟 241→345；真机——Baritone 冒烟 PASS（#goto 3s 收敛 2.0 格）、T5a 原语层 6/6、T6 五项 5/5（collect 提速 4.9 倍至 16.8s/3 根）、T7 四项 4/4（多人服离线回落单机）；T5b 直驱验收：问候 1.6s、砍橡木 4 步 8.0s 16k tokens（M3-C 同意图 22 步 212k 预算耗尽——**步数 1/5.5、token 1/13**，验收标准 ≤4 次/≤30k 达标）。

#### 遇到的问题与处理

1. **gradle wrapper 代理坑**：子任务在新 shell 直接跑 `gradlew build`，联网解析（NeoForge 平台/依赖下载）不走本机代理即失败——gradlew 的 JVM 代理参数必须像 `sirius-bridge/deploy.cmd` 那样显式传（`-Dhttps.proxyHost/-Dhttp.proxyHost` 指向本机代理；PROGRESS 环境备忘既有口径）。处置：构建统一走 `deploy.cmd`（构建+部署一体、参数正确），子代理不再手敲 gradlew。
2. **8765 端口撞车**：真机 bridge 长驻独占 8765 后，`test_agent_loop.py` 里 8 处 `RecordingMock` 固定端口绑定失败（真机与离线测试同机并行是本轮常态）。处置：全部 mock 服务端改 `port=0` 随机空闲端口（T2T4 报告"端口纪律"节），并实测确认测试套件零绑定 8765。
3. **事件层焦点门控发现（本轮最重要真机发现）**：input.click 事件层"按住"在 vanilla 是双门控——`Minecraft.handleKeybinds` 每刻只 `continueDestroyBlock` 当 `mouseHandler.isMouseGrabbed()`，而 `grabMouse()` 在窗口非 active 时拒绝重新抓取；聊天框开/关一次（walkTo 的 #goto 必经路径！）就永久丢抓取，之后注入 PRESS 键态在、零挖掘、零报错（截图实证：准星在原木上无裂纹）。处置：dig 的按住改**动作层**——直接调 vanilla 持键路径自己每刻调用的 `gameMode.startDestroyBlock/continueDestroyBlock/stopDestroyBlock`(+swing)，与 M2-D look 同构（无可靠事件路径时的动作层先例），焦点完全免疫。这是 bridge 边界从"哑管道"修订为三层的实证依据（见 sirius-technical.md §8.3）。
4. **宿主中断两次的处置**：T5a 前任子代理 01:49 被中断一次、后续任务中又一次。三件套应对：① progress 防中断锚点制度化（T6/T7 全程逐段更新 `M3.5-T{6,7}-progress.md`，每段附"怎么续"，主管按最后一节接手）；② 真机探针选择性执行（`python m35_realworld_probe.py 3 4 6` 式，从断点续跑不重复破坏现场）；③ 中断后从真机现状接续（游戏保持运行不重启，避免环境漂移）。两次中断零工作丢失。
5. **world.query 截断排序 bug 铁证（T5a 发现、T6 修复）**：无 filter 查询 range=4.0（count=438 未截断）能看到 3.71 格处的目标，range=5.5 起 count=512 截断后**同目标反而消失**——cap 截断发生在距离排序之前，"truncated 时最近方块仍可信"契约被违反；后果链：dig 的"已不存在"幂等路径把截断误判为真空 → collect 零挖掘 3 次假成功。处置分两步：Python 侧先防御（`_block_at` 未命中仅在未截断时可信，`primitives.py:777-792`）；T6 Java 根治（filtered 路径改"收集→排序→截断"，冒烟加包含律回归断言；无 filter 路径按裁决保留 v1.0 语义，README 注明风险）。
6. 其余真机教训（均已修）：quickPlay 入世后加载屏未消失时 T 键打不开聊天框、#goto 静默丢失（T0b 首轮冒烟 FAIL 的根因 → walkTo 界面屏障）；徒手 oak_log 破坏需连续按住 3.0s（600ms 八段全空 → 基准 3500ms 递增封顶 8000）；转头收敛后被 Baritone 收尾旋转带偏 → aim 卡死（tick 泵失准即重发 turn）；段循环不等 hold 完成就发下一段（迟到 RELEASE 触发 stopAttack 清进度）。

#### 门禁结果

| # | 门禁 | 结果 |
|---|---|---|
| 1 | 代码与文档一致性 | **PASS**（本日完成）：技术规格 §8.2/§8.3/§10.1、overall.md、PROGRESS.md、sirius-bridge/README.md 均按本轮实际接口同步；文档引用的路径/常量逐项核对真实文件 |
| 2 | 跑通验证 | **PASS**：pytest 302/0（起点 263）；`deploy.cmd` 内 gradlew build + smokeTest 345/0（起点 241）；真机 T5a 6/6、T6 5/5、T7 4/4、Baritone 冒烟 PASS、T5b 直驱两任务达标 |
| 3 | tmp 清理 | **PASS（留尾巴一处，有主）**：`%TEMP%\t0b`、`%TEMP%\t6` 已删；`%TEMP%\t7\game_stdout.log` 被运行中游戏进程占用暂不可删，游戏退出后随目录清理（T7-progress 段 3 记录在案） |
| 4 | 当日日志 | **PASS**：本文件收尾日志补全（本节） |
| 5 | 改动摘要 + 小步提交 | **部分完成**：T0b/T1/T2T4/T3 已按逻辑单元提交（4c017a6/257306f/9bd8da2/963f1d9）；T5a/T6/T7 代码+报告在工作区待主管验收后提交（本收尾不含 commit，按约束） |
| 6 | overall.md 维护 | **PASS**：M3.5 章节与已知边界更新（本日同步），代码片段均从真实文件复制核对 |

#### 未完成与下一步

- **M4 范围收窄**：寻路已由 Baritone 集成解决（本轮裁决+真机实证），M4 = **反射层为主**（brain 侧本能链，消费 M2-B CRITICAL 事件）；寻路里程碑内容降为"Baritone 注入路径优化"（#goto 聊天往返 1.3s/次占 collect 大头）
- 系统提示硬约束：本地 VLM 观察类问题不调工具直接幻觉作答（M4）
- 掉落匹配精确 id：stone→cobblestone 不命中需掉落表知识（M4 再议；当前保守不捡恰好符合多人服礼仪）
- `pickup()` 原语未暴露给 VLM（M4 注册表层再议）
- `_block_at` 截断防御已入库，但无 filter 路径仍是 v1.0 cap-before-sort 语义（M4 观察项：若需根治属协议语义变更）
- input.click 事件层长按 / input.mouseMove 转头仍需窗口焦点（vanilla 门控无开关；"AI 播放"场景标准部署 = 游戏窗口保持前台，sirius-bridge/README 已记）
- 多人服在线复验待用户开服（T7 礼仪约束已由代码+单测钉死，单机验证 4/4）
- T5b 完整聊天循环验收待用户进世界（直驱两任务已过）
- hello_ack 未建模（P1 老项，功能不影响）
- collect 16.8s/3 根的下一步提速在换斧/执行器（M5），不在本层
