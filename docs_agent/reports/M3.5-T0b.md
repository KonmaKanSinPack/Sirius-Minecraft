# M3.5 T0b 执行报告：sirius-bridge 部署 + Baritone #goto 冒烟

- 日期：2026-08-20 00:55 – 01:19
- 结论：**BARITONE SMOKE PASS**（第 2 次运行；第 1 次失败原因已定位为探针时序问题，非 Baritone/bridge 缺陷）
- 游戏客户端**保持运行**（PID 27640，供后续任务使用）

## 1. 构建与部署

- 命令：`sirius-bridge/` 下 `cmd //c deploy.cmd`（JAVA_HOME=Microsoft JDK 21.0.12）
- 结果：`BUILD SUCCESSFUL in 10m 16s`，内部 smoke **242 passed / 0 failed**
- 部署：`.minecraft/versions/1.21.1-Sirius/mods/` 现有两个 jar 共存
  - `sirius_bridge-0.1.0.jar`（250,050 B，01:07 部署）
  - `baritone-standalone-neoforge-1.11.2.jar`（1,514,011 B）

## 2. 旧客户端与游戏启动

- 旧客户端（昨夜 1.8GB 实例）开工时已自行退出，无 java 进程，无需 kill。
- 启动命令提取自 HMCL 日志 `2026-08-19T23-02-17.log.xz`（解压后第 43045 行，第二次成功启动的 `Launched process:` 行，16,243 字符）。命令含账号令牌，全程只存在于 repo 外临时目录（`%TEMP%\t0b\`），执行后已删除；未写入任何入库文件。
- 启动方式：命令行超 cmd 批处理 8191 字符上限，故用 Python `subprocess.Popen`（argv 列表、`DETACHED_PROCESS`、cwd=repo 根），末尾追加 `--quickPlaySingleplayer "新的世界"`。
- 游戏进程 PID 27640 于 01:09:50 启动；bridge `START` 日志 01:10:28（启动后约 38s），`127.0.0.1:8765` 监听正常，`config/sirius_bridge.toml` 生成。quickPlay 自动入世成功（getStats `in_game=true`）。

## 3. 冒烟结果

### 第 1 次运行（01:15:46）：FAIL —— 零位移

```
[1] 起点 (4.99, 32.36)（y=63.0），目标列 (25, 32)（#goto GoalXZ）
[2] 已发送 #goto 25 32
    t=1s..41s pos 恒为 (5.0, 63.0, 32.4)，位移=0.0
[3] #stop；最大位移=0.0 格，终点距目标=20.0 格
BARITONE SMOKE FAIL
```

根因（证据链）：
- Baritone **已正常加载**：`latest.log` mod 清单含 `Baritone 1.11.2 (baritoe)`，Mixin Connector 加载成功，`[CHAT] [Baritone] Baritone settings file not found, resetting.`（首次运行正常）。
- 聊天拦截**工作正常**：01:16:33 的 `#stop` 被 Baritone 回显并执行（`[Baritone] > stop` / `ok canceled`）。
- bridge input 工具**工作正常**（两次三连 `input.key T` → `input.text` → `ENTER` 均 result=ok）。
- 差异点：bridge 日志中 01:15:46（#goto）按 T 时 **screen=true**（quickPlay 世界加载覆盖屏尚未消失），文本未进入聊天框，Baritone 从未收到 #goto（无任何回显）；01:16:33（#stop）时 screen=false，一切正常。
- 即：**探针在 getStats in_game=true 后立即发令，与加载覆盖屏消失存在竞态**。判定为探针/时序问题，Baritone 与 bridge 侧无缺陷。

### 第 2 次运行（01:18:30，加载屏已消失）：PASS

```
[1] 起点 (4.991610393102059, 32.35969941810489)（y=63.0），目标列 (25, 32)（#goto GoalXZ）
[2] 已发送 #goto 25 32
    t=1s pos=(11.6,63.0,31.5) 位移=6.7 距目标=13.4
    t=3s pos=(23.1,63.0,31.5) 位移=18.1 距目标=2.0
[3] 已发送 #stop；最大位移=18.1 格，终点距目标=2.0 格

BARITONE SMOKE PASS
```

游戏内聊天回显（repo 根 `logs/latest.log`）：

```
01:18:30.957 [CHAT] [Baritone] > goto 25 32
01:18:30.957 [CHAT] [Baritone] Going to: GoalXZ{x=25,z=32}
01:18:35.272 [CHAT] [Baritone] > stop
01:18:35.274 [CHAT] [Baritone] ok canceled
```

判定满足：位移 18.1 > 5 且终点距目标 2.0 ≤ 3。

## 4. 运维备注（后续任务会用）

- **游戏日志位置取决于启动 cwd**：本次从 repo 根启动，原版 log4j 把 `latest.log`/`debug.log` 写到了 `C:\AI\mc-projects\Sirius-Minecraft\logs\`；而 bridge 走 gameDir，日志在 `.minecraft/versions/1.21.1-Sirius/logs/sirius_bridge.log`（版本目录里还留着上一次运行的旧 `latest.log`，勿混淆）。HMCL 正常启动时 cwd=repo 根同样如此。
- 该日志文件为混合编码，`grep` 会当二进制处理，需加 `-a`。
- 冒烟探针若在 quickPlay 入世后立即运行，建议先等加载覆盖屏消失（或先调一次 getGuiState 确认无 screen）再发 #goto；本次未改 `m35_baritone_probe.py`（任务要求不动源码），重跑即通过。
- 游戏进程 PID 27640 保持运行；其 stdout/stderr 落在 `%TEMP%\t0b\game_stdout.log`（stderr 仅一条无害的 `[nether-pathfinder] Failed to delete temp file`）。
