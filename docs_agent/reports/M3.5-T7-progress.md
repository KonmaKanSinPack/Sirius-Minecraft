# M3.5-T7 进度文件（防中断锚点）

- 任务：修"砍完树不捡掉落物"——bridge entities 载荷补 item 注册名（任务1）+
  collect_block 挖后拾取（任务2，含主管变更：pickup 可配置参数）+ 真机验证（任务3）
- 状态：代码与测试全部完成，进入真机验证（2026-08-20）
- 纪律：每完成一个任务段更新一节；被中断后主管按最后一节"怎么续"接手

## 段 0：调研（已完成）

已读齐：Numen `CollectItemsCompanionTask.java`（磁吸靠 vanilla、实体消失=已拾取、
skip 防死循环、0 件成功）、PerceptionTools/ToolContracts/SmokeMain、primitives.py、
tools.py、loop.py 系统提示、fakeworld.py、test_primitives.py、T0b 启动方法、
T6 报告（部署/冒烟模式）、m35_realworld_probe.py（真机探针基建）。
主管中途变更已并入：collect_block 加 pickup: bool = True，注册表/提示/系统提示联动。

## 段 1：任务1 bridge entities 载荷（已完成）

- `ToolContracts.EntityFact` 加 `item`（nullable String）与 `count`（int）两字段；
  7 参便捷构造器保留（非 item 实体走它，item/count 默认 null/0）
- `filterEntities` 序列化：item != null 才输出 item/count（纯增字段，非 item 实体
  条目形态与 T7 前逐字节一致）
- `PerceptionTools.worldQuery` 实体循环：`entity instanceof ItemEntity` →
  `BuiltInRegistries.ITEM.getKey(stack.getItem())` + `stack.getCount()`（name 是
  客户端本地化中文名不可匹配——与 blocks 返回注册名同哲学）
- 冒烟 +3：item 实体带注册名+count；非 item 实体无这两字段；entities filter 按
  type id 命中 item 实体
- **gradlew build 全绿：smoke 345 passed / 0 failed**（T6 时 342 → +3）；
  deploy.cmd 已把新 jar 部署进 `.minecraft/versions/1.21.1-Sirius/mods/`

## 段 2：任务2 brain 挖后拾取（已完成）

- `primitives.py`：
  - 新常量 DROP_QUERY_RANGE=12 / DROP_NEAR_DIG_DIST=4 / PICKUP_TIMEOUT=20 /
    PICKUP_ARRIVE_DIST=1.2 / PICKUP_SETTLE_SECONDS=3 / PICKUP_MAX_ATTEMPTS=2
  - `_collect_drops(dug_pos, item_ids)`：只捡挖点 4 格内、注册名精确匹配的掉落
    （#tag 条目无法在 item 注册名上展开，保守忽略）；返回捡到件数，0 也正常
  - `_sweep_drops(...)` 核心（Numen 状态机轮询版）：SCAN→APPROACH 循环；走位后
    settle 轮询等吸附；`before - after` 实体集合差 = 已拾取（无论谁捡，路过吸附
    的同家族掉落也计入）；走身旁 ≤1.2 格仍没吸上 / 走不过去 → skip 集合防死循环；
    单实体最多 2 次尝试；超时/无进度即收
  - `pickup(item_ids, radius=12)` 公开方法（Numen collect_items 对应物，0 件成功；
    不注册工具表）；`_query_drops`（filter=[minecraft:item]，无 item 字段条目丢弃
    ——旧 jar 保守不捡）
  - `_collect_block(block_ids, count, pickup=True)`：每次 dig 成功后（pickup=True）
    拾取，匹配集 = 实际挖掉的方块 id + block_ids 纯 id 项；成功话术末尾
    pickup>0 时附"，已捡起 m 个掉落"
- `tools.py`：CollectBlockParams 加 `pickup: bool = True`；collectBlock 参数 JSON 加
  pickup（boolean，可选）；PRIMITIVE_TOOL_HINTS/系统提示（loop.py）补 pickup 指引
  （要物品默认、挖通道/清地形传 false）；handler 透传 pickup；world.query 提示补
  "掉落物实体带 item 注册名与 count"
- **schema 重导出核实为不适用**：collectBlock 是 brain 侧工具，参数 JSON 在
  tools.py 手写，不在 protocol TOOL_PARAMS/export_schema 范围；world.query 的
  item/count 是响应侧载荷（schema 只冻结 params）——协议无变化，无需重导出
  （重跑只会 churn index.json 的 exported_at 时间戳）
- `fakeworld.py`：T7 物源——dig/click 挖掉方块原地生成 item 实体（挖啥掉啥
  count 1）；entities 查询走 item_drops 表（与 Java filterEntities 同口径：range
  平方 3D 距离、filter 按 type、cap 128 + truncated）；getStats/world.query 轮询时
  结算"玩家走近 ~1 格吸附消失"（vanilla playerTouch 近似）；drop_no_absorb 测试
  开关
- **pytest 全绿：302 passed / 0 failed**（T6 时 292 → +10：FakeWorld 掉落物行为 1、
  collect 拾取链 6（主场景/礼仪·不碰别人掉落/挖点 4 格外不捡/实体消失=已拾取/
  skip 防死循环/pickup=False）、pickup() 方法 3（扫匹配/0 件成功/#tag 拒绝）；
  另 test_full_chain 断言更新（话术带拾取注记）、注册表测试补 pickup 断言）

## 段 3：真机验证（已完成——4/4 PASS）

- 开工时游戏在跑但带**旧 jar**（8765 监听，PID 29524→后来自行退出）；09:56 部署新
  jar（build 成功后 deploy.cmd）
- **多人服务器不可达**：上一会话的服务器是 IPv6 link-local LAN 服
  （`fe80::1db1:adc4:7b19:8702:58873`，实例 latest.log 09:38:57 连接记录），
  三个活动接口（zone 38/23/15）TCP 探测全部超时——主机已关。按 T0b 方法回落
  单机世界「新的世界」（拾取机制世界无关；礼仪约束由代码+单测钉死）
- 09:59 启动：HMCL 最新日志 `2026-08-20T09-37-22.log.xz` 提取 Launched process
  命令（含令牌，只存在于 %TEMP%\t7\，已删）→ Python Popen DETACHED +
  `--quickPlaySingleplayer "新的世界"`，PID 20852；bridge START 10:00:28，
  mods 里 sirius_bridge-0.1.0.jar 时间戳 09:56 = 新 jar
- 首跑探针发现玩家**死亡屏**（health=0，上一会话遗留状态）挡住聊天：getGuiState
  拿到 DeathScreen 按钮树（gui-scaled 重生键中心 (213,142)，scale 3 → 窗口像素
  (639,426)），input.mouseMove+input.click 重生成功（health 20）
- 探针 `m35_t7_pickup_probe.py`（repo 根，venv python 跑；系统 Python 的
  websockets 版本不匹配会报 reconnect_delays 错）结果：

```
[1 entities载荷] PASS — count=1 首条字段=['count','item','name','position','type','uuid'] item=minecraft:oak_log n=1
[2 collect拾取] PASS — 8.3s 话术="已挖到 1/1 个 minecraft:oak_log，已捡起 1 个掉落" 掉落实体 0→0
[3 pickup=False] PASS — 话术="已挖到 1/1 个 minecraft:oak_log" 掉落实体 0→1
pickup() 单测 — 话术="已捡起 1 个 minecraft:oak_log"，掉落实体 [('minecraft:oak_log',1)] → []
```

  （item 1 首跑 FAIL 只因当时地上没有掉落物可查；item 3 留下的掉落物使复跑通过
  ——顺序执行 3→1 即可复现全绿）

- 游戏保持运行（PID 20852，quickPlay 已在世，供主管复验）；stdout 落
  %TEMP%\t7\game_stdout.log（进程占用暂不可删，游戏退出后随目录清理）

## 段 4：报告（已完成）

交接报告：`docs_agent/reports/M3.5-T7.md`
